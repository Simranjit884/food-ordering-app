const BackDrop = (props) => {
  return <div></div>;
};
const OverLay = (props) => {
  return (
    <div>
      <div>{props.children}</div>
    </div>
  );
};

const Modal = (props) => {
  return <div></div>;
};

export default Modal;
